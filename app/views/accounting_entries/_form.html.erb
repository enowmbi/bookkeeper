<%= form_with(model: accounting_entry, local: true) do |form| %>
  <% if accounting_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(accounting_entry.errors.count, "error") %> prohibited this accounting_entry from being saved:</h2>

      <ul>
        <% accounting_entry.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date_prepared %>
    <%= form.date_select :date_prepared %>
  </div>

  <div class="field">
    <%= form.label :date_posted %>
    <%= form.date_select :date_posted %>
  </div>

  <div class="field">
    <%= form.label :memo %>
    <%= form.text_area :memo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
